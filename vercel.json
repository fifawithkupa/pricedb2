{
  "buildCommand": "mkdir -p dist && cp index.html Landing.html dist/ && cp -R ds dist/ds",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/",
      "destination": "/index.html"
    }
  ]
}
